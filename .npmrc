# Skip optional platform-native binaries (e.g. rollup/esbuild native modules
# bundled inside netlify-cli). Without this they end up as extraneous entries
# in the lockfile and cause EBADPLATFORM errors on mismatched CI platforms.
omit=optional
