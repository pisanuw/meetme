[build]
  command = "npm install"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Explicit fallback for unknown routes to custom 404 page.
# This keeps deep-link mistakes and old URLs user-friendly in production.
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
